<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 960 960">
  <!-- Transparent background explicitly -->
  <rect width="960" height="960" fill="none" fill-opacity="0"/>
  <!-- Frame (solid black) -->
  <path d="M220 520 L360 380 L600 380 L760 520 L700 580 L560 440 L400 440 L260 580 Z" fill="#000000"/>
  <!-- Rear wheel (ring style) -->
  <circle cx="270" cy="620" r="150" fill="none" stroke="#000000" stroke-width="60"/>
  <!-- Front wheel (ring style) -->
  <circle cx="690" cy="620" r="150" fill="none" stroke="#000000" stroke-width="60"/>
  <!-- Motor housing as solid black circle (no opacity) -->
  <circle cx="480" cy="620" r="70" fill="#000000"/>
  <!-- Handlebar -->
  <path d="M600 380 L740 270" stroke="#000000" stroke-width="60" fill="none" stroke-linecap="round"/>
  <!-- Seat -->
  <path d="M400 380 L330 330" stroke="#000000" stroke-width="60" fill="none" stroke-linecap="round"/>
</svg>